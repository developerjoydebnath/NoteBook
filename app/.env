# In Expo development, use your computer's IP address instead of localhost
# If using --tunnel, you might need a tunnel for your server (e.g. ngrok) if local network isn't reachable
EXPO_PUBLIC_API_URL=http://192.168.1.108:5000/api
